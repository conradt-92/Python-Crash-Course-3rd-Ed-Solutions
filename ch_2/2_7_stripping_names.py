# Conrad T
# Stores a name with whitespace in the variable name.
# Prints the name with its original formatting.
# Prints a stripped version of the name using the lstrip, rstrip, and strip methods.

name = "\t conrad t \n "

print(name)

print(name.lstrip())
print(name.rstrip())
print(name.strip())